services:
    backend:
        build:
            context: .
            dockerfile: apps/backend/Dockerfile
        environment:
            DATABASE_URL: postgresql://admin:admin@postgres:5432/tms_db
        ports:
            - "3000:3000"
